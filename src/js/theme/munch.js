// JavaScript to handle the infinite scrolling effect
/*const infiniteScroll = document.getElementById("infinite-scroll");

infiniteScroll.addEventListener("scroll", () => {
  // Check if the user has scrolled to the bottom of the element
  if (infiniteScroll.scrollTop + infiniteScroll.clientHeight >= infiniteScroll.scrollHeight) {
    // Load more content dynamically using AJAX or Fetch API
    console.log("Load more content here...");
  }
});
*/
